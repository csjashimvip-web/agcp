NODE_ENV=production

# Prefer a dedicated Laravel API hostname in staging.
NEXT_PUBLIC_API_BASE_URL=https://api-staging.example.com

# The Next.js process listens only on loopback.
HOSTNAME=127.0.0.1
PORT=3000