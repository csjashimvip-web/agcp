# Integrations
Signed outbound webhooks with event subscriptions, tenant-scoped endpoints, replay-safe event IDs, exponential retries, dead-letter visibility and SSRF-resistant HTTPS validation. Local development supports a `log://` sink.
